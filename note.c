#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
Touched by my second account!!

int main()
{
	int a, b;
	printf("input a number : ");
	scanf("%d", &a);
	printf("input a number : ");
	scanf("%d", &b);

	int sum; 
	sum = a + b;
	int mul ;
	mul = a * b
	
	printf("the sum is %d\n", sum);
	printf("the mul is %d\n", mul);

	return 0;
}
